<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wuyi.mall.mapper.MerchantApplyMapper">

    <!-- 根据状态查询商家申请列表（分页） -->
    <select id="selectByStatus" resultType="com.wuyi.mall.entity.MerchantApply">
        SELECT * FROM merchant_apply
        WHERE 1=1
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="startTime != null">
            AND create_time &gt;= #{startTime}
        </if>
        <if test="endTime != null">
            AND create_time &lt;= #{endTime}
        </if>
        ORDER BY create_time DESC
    </select>

    <!-- 检查商家名称是否已存在 -->
    <select id="checkMerchantNameExists" resultType="java.lang.Integer">
        SELECT COUNT(1) FROM merchant_apply WHERE merchant_name = #{merchantName}
    </select>

    <!-- 检查营业执照编号是否已存在 -->
    <select id="checkBusinessLicenseExists" resultType="java.lang.Integer">
        SELECT COUNT(1) FROM merchant_apply WHERE business_license = #{businessLicense}
    </select>

</mapper>
